package com.catalin.vibelog.model.enums;

/**
 * Defines the privilege levels available to an Admin user.
 */
public enum AdminLevel {
    /** Basic admin with limited permissions. */
    BASIC,
    /** Full admin with standard administrative permissions. */
    FULL,
    /** Super admin with all permissions, including advanced system controls. */
    SUPER
}
